{
  "name": "@thinkroid/agent-core",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Per-agent daemon for the Thinkroid-Space platform (Brain + Context Engine + Cerebellum + per-agent SQLite).",
  "main": "src/kernel.js",
  "bin": {
    "agent-core-daemon": "bin/agent-core-daemon.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  },
  "scripts": {
    "test": "vitest run tests/lib"
  }
}
